import React from 'react';

const Homepage = () => (
  <div className={`bg-home-background-mobile bg-cover bg-center h-screen relative flex flex-col opacity-75  ${'md:bg-home-background'}`} style={{ backgroundColor: 'rgba(0, 0, 0, 0.6)' }}>
    Homepage
  </div>
);

export default Homepage;
